#include <iostream>
#include <string>

using namespace std;

int main() {
    string line;
    getline(cin, line); // 从标准输入流中读入一行数据
    cout << "输入的字符串为: " << line << endl;
    return 0;
}
//gpt助写